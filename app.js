// 주석
console.log(234);

const player = {
    name: "기용",
    points: 100,
    alive: true
};

console.log(player.name);

player.lasName = "정"

// object 전체만 constant
// 안의 각각 정보는 let으로 저장

console.log(player["name"])
